export type Data=[
    {
        id: Number,
        role: String,
        name: String
    }
]

export type Info = {
    id: number|string,
    name: string,
    start: number|string,
    finish: number|string,
    role: string,
    actions: any
}